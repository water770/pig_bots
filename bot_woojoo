def choice(round_score, my_score, opponent_score):
  winning_threshold = 80
  if round_score == 1 or my_score + round_score >= winning_threshold:
    return false
  if my_score < opponent_score:
    return round_score < 15
  else:
    return round_score < 12
